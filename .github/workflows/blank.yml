name: Hello World
on:
  push:
    branches:
      - master
      - develop
      - 'release/**'
  pull_request:
    branches:
      - master
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
     - name: build step
       run: echo "Hello World"

 
